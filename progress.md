# Slice progress

Status: `not_started` | `in_progress` | `p0_junit` | `p0_client` | `done`

`done` requires P0 pass. `skip` is not pass. See `spec/07-rebuild/test-plan.md`.
JUnit: `mvn -f tbc-server/pom.xml test` (TP-INV, TP-NEG, SL01–03 P0, matrix, Gherkin slices 4–11, Slice14P0Test–Slice30P0Test, `Slice04LogoutTest`, `PvpObjectivesTest` 24–25, `LaterP0Test` leftover, `SpecFilesP0Test` 31–34).

Sidecar: `tbc-admin` is a Swing operator tool for `tbcrealmd.account` (create / role / password / delete). It is **not** a slice, **not** SOAP/RA, and **not** `p0_client`. Passwords are stored as SRP6 `v`/`s` only.

Official 8606 client **entered world** (lab 2026-09-02). Slice 4 is `p0_client`, not `done`: two-client Gherkin (`TP-SL04-003/004/005/010`) is **p0_junit**. Observer move/name (`003`/`005`) still need a two-client 8606 lab before `done`; do not tick `p0_client` on `004`/`010` until that lab click. `CMSG_PLAYER_LOGOUT` is a **documented no-op** (`logout.md`); `Slice04LogoutTest` asserts no logout S2C. Deepened `CMSG_LOGOUT_REQUEST`: duel / `MOVEFLAG_FALLINGFAR` → reason `1`; taxi flight → instant logout. Still covers empty `SMSG_CONTACT_LIST`, combat-block, gmlevel-0 delayed response, logout/re-enter.

Slice 4 requirements stack is complete (nothing to invent): vision → `FR-MOV-001/002`, `FR-WLD-005`, `FR-SOC-001` → US-013 / US-020 / US-021 → `TP-SL04-003/004/005/010` → `movement.md` / `chat.md` / `queries.md` → `WorldSession` `handleMove` / `handleChat` / `handleNameQuery` / `revealNearby`. Later-slice YAML `us: []` is spec hygiene, not a slice-4 blocker. US-020 “no falling through vmaps” has no TP and is untestable in CI without DataDir vmaps — do not add collision here. Slices 5–34 P0 are **p0_junit** (Gherkin / `LaterP0Test` / spec-file tests). None of 5–34 are `done` or `p0_client` except where a live 8606 click is already noted. Official two-client lab for SL04-003/005 is still open (**Loop B**, not CI).

Lab: account **REMI** (id 1007, expansion 1), character **piep** (guid 9032, gnome warrior, Coldridge). Auth `proof ok`; `CMSG_PLAYER_LOGIN` reached in-world. Starter outfit from `CharStartOutfit.dbc`. Live slices 5–8: `CharacterStore.save` is one SQL transaction; map 0/1 `creature` rows spawn and nearby units are sent as `UPDATE_OBJECT`. `.tele x y z [map]` (SEC_MODERATOR) for Goldshire. Live 8606: melee range/HP/corpse and warrior rage + Heroic Strike **78** on piep. Slice 9: two 8606 clients opened the trade window. Chat language: after create-self the burst now sends a skill VALUES block plus `SMSG_LEARNED_SPELL` for Common **668** / Gnomish **7340** so the 8606 language menu can refresh. Relog required after world restart.

| Slice | Status | Notes |
|------:|--------|-------|
| 0 scaffold | done | Maven reactor, conf, AGENTS.md, Cursor rule |
| 1 Auth | p0_client | Live 8606 SRP6 + realm list. P0 JUnit: wrong password, ban, IP ban, IP lock, sessionkey, build 8606. Not `done` (lab already entered realm select). |
| 2 World session | p0_client | Live 8606 character screen. P0 JUnit: addon-then-AUTH_OK + expansion 1, OS reject, reconnect, overspeed kick (27 s, SEC_PLAYER). |
| 3 Characters | p0_client | Live create/enum **piep**. P0 JUnit: Alliance+Horde+duplicate **0x32**, BE expansion **0x39**, delete **0x3B**, guild-leader **0x3E**. |
| 4 Enter world / move / chat | p0_client | **`TP-SL04-001` pass** (8606 in starting zone). Two-double Gherkin **p0_junit**: observer `MSG_MOVE_HEARTBEAT` packed GUID + `fallTime` (`003`), say type `0x01` (`004`), other-player create without `UPDATEFLAG_SELF` + name query (`005`), whisper `0x07`/`0x09` (`010`). Not `done` until 003/005 are labbed. JUnit: login-burst, UPDATEFLAG_SELF 0x71, empty friends list, MovementInfo, say/whisper, logout handshake (duel/fallingfar block, taxi instant, `CMSG_PLAYER_LOGOUT` documented no-op), time-sync. Live `/say` works after language-skill relog. |
| 5 Persistence | p0_junit | Gherkin + `WowClientDouble`. SQL `save` is one transaction (characters + actions + inventory savepoint). `clearOnline` on world start. Live relog/vendor-buy on piep still needs a client click. **Not `p0_client`.** |
| 6 Combat | p0_junit | Gherkin + `WowClientDouble`. Live: `creature_template` SELECT uses ModelId1 and quoted Rank (MySQL reserved); map 0/1 `creature` rows instantiated; nearby `UPDATE_OBJECT` on login/move. Attack/loot/evade on piep still needs a client click. **Not `p0_client`.** |
| 7 Spells | p0_junit | Gherkin Fireball 133 + JUnit Heroic Strike **78**. SCHOOL_DAMAGE magic miss base **4%** (`SMSG_SPELLLOGMISS` + `SMSG_SPELL_GO` miss row; in-memory always-hit so `@tp-sl07-001` stays deterministic). Lab 8606: rage bar, melee rage, HS 78 on piep (still not `p0_client` in the two-client sense; single-client HS click done). **Not `done`.** |
| 8 Content | p0_junit | Gherkin Goldshire NPC 240, vendor 54, quest 783. Live NPCs come from map-0 spawns; `.tele` to Goldshire (`-9465 16 57`). Continent `gameobject` rows instantiate on map 0/1 (event GOs stay off that list). Gossip/quest on 8606 still needs a client click. **Not `p0_client`.** |
| 9 Social / economy | p0_junit | Gherkin two doubles: invite/`SMSG_GROUP_LIST` (HIGHGUID_GROUP 0x1F50), party chat 0x02, trade both-accept, who display cap 49, `character_social` + `SMSG_CONTACT_LIST` after relog, mail take-item **uint32** guid-low. Live 8606: trade window opens (REMI/REMI2). Party/mail still need a client click. **Not `p0_client`.** |
| 10 Instances / PvP | p0_junit | Gherkin RFC AT **2230** map 389 shared bind + reset; WSG WAIT_JOIN **2** then port map **489**. **Not `p0_client`.** |
| 11 EventAI | p0_junit | Gherkin Garrick **103** spell **7164** (`TP-SL11-001`); `FactorySelector.selectAI` (ScriptName / pet / AIName / Guard / permit EventAI / Null); creature melee after `BASEATTACKTIME` in range; straight-line MotionMaster CHASE + `SMSG_MONSTER_MOVE` (no mmaps); RANGE / OOC_LOS with vmap-off LOS clear; `EventAiStore` JDBC `creature_ai_scripts` (103 seed if empty); dbscripts CAST **15** on creature death, delay queue + TERMINATE **31** (no buddy search). **Not `p0_client`.** |
| 12 Combat math | p0_junit | `MeleeTableTest` 5% miss/dodge/parry/block vs same-level NPC; yellow-NPC miss; miss vs player `× 0.04`; dual-wield white +19 miss unless next-melee queued; NPC dodge 0.1 / parry 0.6 skill; NPC block no +skill when defense ahead; player avoid from `PLAYER_*_PERCENTAGE` (no +5); expertise `× 0.25` off dodge/parry (`PLAYER_OFFHAND_EXPERTISE` on offhand); offhand crit from `PLAYER_OFFHAND_CRIT_PERCENTAGE`; totem victim base miss 0; partial block subtracts `PLAYER_SHIELD_BLOCK` and `SMSG_ATTACKERSTATEUPDATE` blocked uint32 is the remainder; player vs NPC>10 glance 10+(defense−skill), wand-user <30 `level+(defense−skill)`; glance damage 1.2/1.3 or caster 0.9/0.6; sitting player remaining table is crit; player/creature crit 2×; NPC crush 150% when skill deficit ≥ 15; evade mode is `MELEE_HIT_EVADE` (`hitInfo` MISS\|SWINGNOHITSOUND, `VictimState` EVADES); swing damage from `UNIT_FIELD_MINDAMAGE`/`MAXDAMAGE`; offhand from `MIN/MAXOFFHANDDAMAGE` without consuming next-melee; `hitInfo` CRITICALHIT / CRUSHING / NOACTION on next-melee spell swing / LEFTSWING on offhand (kept on miss); dual-wield offhand auto-attack after `UNIT_FIELD_BASEATTACKTIME+1` (`HITINFO_LEFTSWING`); melee range `ATTACK_DISTANCE` 5 + `UNIT_FIELD_COMBATREACH`; `ThreatManager` per-attacker float, white melee ×1, highest guid retarget, reset on evade; Heroic Strike **78** queues next-melee with SpellInfo weapon bonus; the swing consumes it and adds that bonus to the weapon range |
| 13 Spell catalogs | p0_junit | `SpellEngine.knownEffect` + dummy/script no crash. SCHOOL_DAMAGE Fireball **133** uses magic miss **4%**, not the melee die (`TP-SL07-001` / `TP-SL13-001`). LEARN_SPELL adds `SpellInfo.misc` (Fireball **133**) to the player's spell book. CREATE_ITEM adds Worn Shortsword **25** and `SMSG_ITEM_PUSH_RESULT` with `created=1`. TRIGGER_SPELL nests cataloged Fireball **133** (no GCD). `CMSG_CANCEL_AURA` unapplies UA **30108**. UA **30108** Amplitude tick sends `SMSG_PERIODICAURALOG` (`PERIODIC_DAMAGE` **3**, damage = health delta). Lesser Heal **2050** crit uses `PLAYER_SPELL_CRIT_PERCENTAGE1` at **1.5×**. |
| 14 Living world | p0_junit | Wire: `Slice14P0Test` swap VALUES (`TP-SL14-001`) including `CMSG_SWAP_ITEM` backpack, destroy Worn Shortsword **25**, split **25**, Olivia **2455** `SMSG_SHOW_BANK`, autobank/autostore **25**, autoequip **25** to mainhand **15**, autoequip Small Brown Pouch **4496** `SMSG_OPEN_CONTAINER`, autostore bag **25** to next backpack slot, set ammo Rough Arrow **2512** `PLAYER_AMMO_ID`, read Dusty Unsent Letter **889** (`PageText` **16**) `SMSG_READ_ITEM_OK` raw guid, open Dented Crate **6351** `SMSG_LOOT_RESPONSE` pickpocketing **2**, wrap Worn Shortsword **25** with Red Ribboned Wrapping Paper **5042** (`ITEM_DYNFLAG_WRAPPED`, consume 1 from a stack; equipped mainhand **15** is ignored; stack count ≠ 1 is ignored; already wrapped is ignored; Small Brown Pouch **4496** bag is ignored; soulbound is ignored; unique MaxCount is ignored), open wrapped **25** unwraps (no loot), `CMSG_CANCEL_TEMP_ENCHANTMENT` mainhand **15** clears `TEMP_ENCHANTMENT_SLOT` (no reply), Llane **6673** (`002`), Dungar taxi (`003`), Elwynn weather (`004`). `GameEventMgr.start`/`stop` Midsummer **1** spawns/despawns Luma Skymother **25697** (SQL `game_event_creature` guid **5470211**, else seed guid 11) and Ice Stone **187882**; SQL `game_event_gameobject` Ice Block **188067** guid **5470021**. Positive event guids stay off the continent spawn list until `start`. GO-only events still spawn (Midsummer Ice Stone **187882** without a creature list). Continent `gameobject` rows instantiate on map 0/1 (Ice Stone **187882** guid **5470020**). Buy bank slot / tick schedule still open. Not `p0_client`. |
| 15 Social leftover | p0_junit | Wire: `Slice15P0Test` NBG roll timeout 60000 (`TP-SL15-001`), raid `GROUP_LIST` + ready-check requester guid (`002`), roster gender (`003`), guild bank `SMSG_GUILD_BANK_LIST` (`004`), Chilton 8670 `BuildAuctionInfo` + delay 300 (`005`). Not `p0_client`. |
| 16 PvP leftover | p0_junit | Wire: `Slice16P0Test` arena `SMSG_NEW_WORLD` ∈ {559,562,572} (`001`), Hellfire WS **2480**/2476/2478 (`002`), LFG packed-guid list (`003`), WSG WS **1545** + aura **23333** (`004`). Not `p0_client`. |
| 17 Death | p0_junit | Wire: `Slice17P0Test` GY `SMSG_DEATH_RELEASE_LOC` + delay 30000 (`001`), reclaim 50% no sickness (`002`), spirit healer **15007** + 25% durability (`003`). Not `p0_client`. |
| 18 Pets | p0_junit | Wire: `Slice18P0Test` `SMSG_PET_SPELLS` pet guid (`001`), stable **0x0A/0x08/0x09** (`002`), warlock dismiss vs hunter (`003`), totem `SMSG_DESTROY_OBJECT` (`004`). |
| 19 Channels | p0_junit | Wire: `Slice19P0Test` YOU_JOINED **0x02** + channelId (`001`), list raw guid (`002`), text emote (`003`), voice ignored (`004`). |
| 20 Items leftover | p0_junit | Wire: `Slice20P0Test` buyback slot **74** VALUES (`001`), socket bonus vs meta **25890** (`002`), repair coinage (`003`). |
| 21 Guild leftover | p0_junit | Wire: `Slice21P0Test` guild query name (`001`), `MSG_GUILD_PERMISSIONS` 6 tab pairs (`002`), roll + ping guid (`003`). |
| 22 Honor / inspect / duel | p0_junit | Wire: `Slice22P0Test` `SMSG_PVP_CREDIT` + cap 75000 (`001`), inspect packed + 0x3D (`002`), duel **3000** + OOB (`003`). |
| 23 Arena teams | p0_junit | Wire: `Slice23P0Test` arena roster personalRating (`001`), PvP log type 0 (`002`), AFK **43680** from 3 unique reporters (`003`). |
| 24 AV / AB / EY | p0_junit | AB stables/blacksmith WS; EY flag score; AV Snowfall; Irondeep mine tick **+1 / 45s** WS **3127**. |
| 25 Outdoor PvP | p0_junit | Silithyst; Northpass; ZM East; Halaa **182210** WS **2673**; TF five towers lock WS **2767** + **33377** (`TP-SL25-002`). |
| 26 Spell algorithms | p0_junit | Door anim; chest open-lock; energize; add honor **45**; talent wipe; cancel channel; `CMSG_CANCEL_AURA` removes UA **30108**. |
| 27 Transports | p0_junit | Board MO + pathProgress; `leaveMoTransport`; ONTRANSPORT echo; ACK; mount. |
| 28 Misc packets | p0_junit | Wire: `Slice28P0Test` quest push guid (`001`), master loot `SMSG_ITEM_PUSH_RESULT` (`002`), GMTICKET **0x06** (`003`), LFG accept `SMSG_LFG_UPDATE` (`004`). |
| 29 GM commands | p0_junit | `Slice29P0Test`: `.help`/`.dismount` SEC_PLAYER; `.appear` LowerSecurity; SQL overlay. |
| 30 ScriptDevAI registry | p0_junit | `Slice30P0Test`: `boss_gruul` Growth **36300**; missing ScriptName fallback. |
| 31 TBC raids | p0_junit | Curator **30383**; Magtheridon; Karathress; Al'ar; Anetheron **31306** / 20s; Najentus **39837** (`TP-SL31-007`). |
| 32 TBC 5-mans | p0_junit | Spec file existence for HFC/CF/Auch/TK/CoT |
| 33 Classic raids | p0_junit | MC/BWL/AQ/Naxx/ZG/Onyxia + `world-remaining.md` |
| 34 Class scripts | p0_junit | Execute **5308→20647**; UA **30108**; spec grep |

## Loop B — official 8606 two-client lab

Started after Loop A wire oracles for slices 14–16. GitHub Actions runs `mvn test` only; it does not launch `Wow.exe`. Next lab click: two 8606 clients for `TP-SL04-003` (observer `MSG_MOVE_HEARTBEAT`) and `TP-SL04-005` (other-player create + name query). Until that lab, slice 4 stays `p0_client` not `done`. Do not tick `p0_client` on later slices from JUnit alone.
