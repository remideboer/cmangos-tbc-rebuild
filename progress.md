# Slice progress

Status: `not_started` | `in_progress` | `p0_junit` | `p0_client` | `done`

`p0_junit` means the wire (`WowClientDouble` / Gherkin) proves what a player would see for that slice’s Milestone and Build — not that some YAML id was automated. `done` requires that plus, from slice 4, a real 8606 click (`p0_client`). Named P0 is necessary; a short YAML list is not a cap. `skip` is not pass. See `spec/07-rebuild/test-plan.md`.
JUnit: `mvn -f tbc-server/pom.xml test` (TP-INV, TP-NEG, SL01–03 P0, matrix, Gherkin slices 4–11, Slice14P0Test–Slice30P0Test, `Slice04LogoutTest`, `PvpObjectivesTest` 24–25, `LaterP0Test` leftover, `SpecFilesP0Test` 31–34).

Sidecar: `tbc-admin` is a Swing operator tool for `tbcrealmd.account` (create / role / password / delete). `tbc-editor` is a Swing operator tool for character CRUD (`tbccharacters.characters`, account names from `tbcrealmd.account`). `tbc-launcher` is a Swing operator tool (Launch4j `TbcLauncher.exe`) that start/stop/restarts auth+world and opens admin/editor. None of these are a slice, SOAP/RA, or `p0_client`. Admin passwords are stored as SRP6 `v`/`s` only.

Official 8606 client **entered world** (lab 2026-09-02). Slice 4 is `p0_client`, not `done`: two-client Gherkin (`TP-SL04-003/004/005/010`) is **p0_junit**. Observer move/name (`003`/`005`) still need a two-client 8606 lab before `done`; do not tick `p0_client` on `004`/`010` until that lab click. `CMSG_PLAYER_LOGOUT` is a **documented no-op** (`logout.md`); `Slice04LogoutTest` asserts no logout S2C. Deepened `CMSG_LOGOUT_REQUEST`: duel / `MOVEFLAG_FALLINGFAR` → reason `1`; taxi flight → instant logout. Still covers empty `SMSG_CONTACT_LIST`, combat-block, gmlevel-0 delayed response, logout/re-enter.

Slice 4 requirements stack is complete (nothing to invent): vision → `FR-MOV-001/002`, `FR-WLD-005`, `FR-SOC-001` → US-013 / US-020 / US-021 → `TP-SL04-003/004/005/010` → `movement.md` / `chat.md` / `queries.md` → `WorldSession` `handleMove` / `handleChat` / `handleNameQuery` / `revealNearby`. Later-slice YAML `us: []` is spec hygiene, not a slice-4 blocker. US-020 “no falling through vmaps” has no TP and is untestable in CI without DataDir vmaps — do not add collision here. Slices 5–34 P0 are **p0_junit** (Gherkin / `LaterP0Test` / spec-file tests). None of 5–34 are `done` or `p0_client` except where a live 8606 click is already noted. Official two-client lab for SL04-003/005 is still open (**Loop B**, not CI).

**Gap inventory:** `spec/07-rebuild/gap-inventory.md` lists every player-visible behavior still missing per slice (Tier A core loop, Tier B silently dropped C2S, Tier C lab), each with a `TP-*` in `test-cases.yaml`. Fill order: Tier A slices 4→8 first (stats/mana/XP bars, regen, death-from-damage, kill XP/level-up, cast time, cooldowns, aura slots, quest objectives), then Tier B, then resume the slice 26 aura catalog. A slice row below saying "Build filled" is superseded by an open row in the inventory.

Lab: account **REMI** (id 1007, expansion 1), character **piep** (guid 9032, gnome warrior, Coldridge). Auth `proof ok`; `CMSG_PLAYER_LOGIN` reached in-world. Starter outfit from `CharStartOutfit.dbc`. Live slices 5–8: `CharacterStore.save` is one SQL transaction; map 0/1 `creature` rows spawn and nearby units are sent as `UPDATE_OBJECT`. `.tele x y z [map]` (SEC_MODERATOR) for Goldshire. Live 8606: melee range/HP/corpse and warrior rage + Heroic Strike **78** on piep. Slice 9: two 8606 clients opened the trade window. Chat language: after create-self the burst now sends a skill VALUES block plus `SMSG_LEARNED_SPELL` for Common **668** / Gnomish **7340** so the 8606 language menu can refresh. Relog required after world restart.

| Slice | Status | Notes |
|------:|--------|-------|
| 0 scaffold | done | Maven reactor, conf, AGENTS.md, Cursor rule |
| 1 Auth | p0_client | Live 8606 SRP6 + realm list. P0 JUnit: wrong password, ban, IP ban, IP lock, sessionkey, build 8606. Not `done` (lab already entered realm select). |
| 2 World session | p0_client | Live 8606 character screen. P0 JUnit: addon-then-AUTH_OK + expansion 1, OS reject, reconnect, overspeed kick (27 s, SEC_PLAYER). |
| 3 Characters | p0_client | Live create/enum **piep**. P0 JUnit: Alliance+Horde+duplicate **0x32**, BE expansion **0x39**, delete **0x3B**, guild-leader **0x3E**. `TP-SL03-006`: `CMSG_CHAR_RENAME` with `AT_LOGIN_RENAME` → `SMSG_CHAR_RENAME` **0x00** + guid + name; enum `CHARACTER_FLAG_RENAME` **0x4000**; duplicate/no-flag → `CHAR_CREATE_ERROR` **0x30**. Latin `CMSG_SET_PLAYER_DECLINED_NAMES` → result **1** (not Cyrillic). Cyrillic persist: result **0** + `SMSG_NAME_QUERY_RESPONSE` declinedFlag **1** and five cases (`CheckDeclinedNames` / `character_declinedname`). |
| 4 Enter world / move / chat | p0_client | **`TP-SL04-001` pass** (8606 in starting zone). Two-double Gherkin **p0_junit**: observer `MSG_MOVE_HEARTBEAT` packed GUID + `fallTime` (`003`), say type `0x01` (`004`), other-player create without `UPDATEFLAG_SELF` + name query (`005`), whisper `0x07`/`0x09` (`010`). Not `done` until 003/005 are labbed. `TP-SL04-011`: create-self VALUES from `player_classlevelstats` / `player_levelstats` (`LevelStats`, `Player.initStatsForLevel`; human warrior 20 base hp → 60 max with stamina, stats 23/20/22/20/20, armor agi×2). `TP-SL04-012`: mana classes get `MAXPOWER1`/`POWER1` = basemana + intellect bonus (human mage 165), `power1` loaded from SQL and kept across snapshots. `TP-SL04-013`: `PLAYER_NEXT_LEVEL_XP` from `player_xp_for_level` (400 at level 1) and `PLAYER_XP` from `characters.xp` in the self block. `TP-SL04-014/015`: `Player.regenerateAll` every 2000 ms from `WorldSession.tick` (health from spirit × gtOCTRegenHP, mana √int × spirit × gtRegenMPPerSpt with the 5 s rule, ×1.5 sitting, rage −2.5 OOC); gt*.dbc from DataDir, level-1 seeds in memory. `TP-SL04-018`: rogue `MAXPOWER4`/`POWER4` 100, +20 energy per tick (also in combat), `power4` persisted. `TP-SL04-016` `/played`: `CMSG_PLAYED_TIME` → `SMSG_PLAYED_TIME` total+level u32 (new char **0**/**0**). Helm hide: `CMSG_SHOWING_HELM` toggles `PLAYER_FLAGS_HIDE_HELM` **0x400** VALUES. Realm split: `CMSG_REALM_SPLIT` → `SMSG_REALM_SPLIT` state **0** / date `01/01/01`. Cloak hide: `CMSG_SHOWING_CLOAK` toggles `PLAYER_FLAGS_HIDE_CLOAK` **0x800**. Account data: `CMSG_UPDATE_ACCOUNT_DATA` / `CMSG_REQUEST_ACCOUNT_DATA` round-trip (`SMSG_UPDATE_ACCOUNT_DATA` type+len+zlib, in-memory). Action bars: `CMSG_SET_ACTIONBAR_TOGGLES` writes `PLAYER_FIELD_BYTES` byte **2**. Tutorials: `CMSG_TUTORIAL_CLEAR` → next login `SMSG_TUTORIAL_FLAGS` eight **0xFFFFFFFF**. Tutorial reset: `CMSG_TUTORIAL_RESET` → eight **0**. Taxi benchmark: `CMSG_SET_TAXI_BENCHMARK_MODE` sets/clears `PLAYER_FLAGS_TAXI_BENCHMARK` **0x20000**. `TP-SL04-017`: `CMSG_EMOTE` WAVE **3** → `SMSG_EMOTE` to self and nearby. `CMSG_KEEP_ALIVE` is WorldSocket OnRead (not queued). `CMSG_OPT_OUT_OF_LOOT` STATUS_AUTHED uint32, no SMSG (activation not implemented in this tree). JUnit: login-burst, UPDATEFLAG_SELF 0x71, empty friends list, MovementInfo, say/whisper, logout handshake (duel/fallingfar block, taxi instant, `CMSG_PLAYER_LOGOUT` documented no-op), time-sync. Live `/say` works after language-skill relog. |
| 5 Persistence | p0_junit | Gherkin + `WowClientDouble`. SQL `save` is one transaction (characters + actions + inventory savepoint). `clearOnline` on world start. `TP-SL05-006`: periodic jittered first save. `TP-SL05-007`: TCP close is `SetOffline` + 60 s then `LogoutPlayer` save. `TP-SL05-008`: startup deletes dangling item refs at `item >= MAX(item_instance.guid)+1`. Live relog/vendor-buy on piep still needs a client click. Slice 5 Build is filled. **Not `p0_client`.** |
| 6 Combat | p0_junit | Gherkin + `WowClientDouble`. Live: `creature_template` SELECT uses ModelId1 and quoted Rank (MySQL reserved); map 0/1 `creature` rows instantiated; nearby `UPDATE_OBJECT` on login/move. `TP-SL06-003`: creature `SMSG_ATTACKSTART` (raw GUIDs) when the player swings in melee. `TP-SL06-018`: out of melee `CMSG_ATTACKSWING` starts only the player (`SMSG_ATTACKSTART` player→creature + `NOTINRANGE`); hostiles still pull via detection (`TP-SL06-005`); neutrals do not pull from the click. `TP-SL06-019`: unreachable (Z) starts a 10 s evade timer (`MELEE_HIT_EVADE` on the wire) then `EnterEvadeMode`; outdoor leash is pursuit 15 s then `LeashRadius` 30 from last damage refresh (CMaNGOS `CombatManager`), not instant spawn+30. `TP-SL06-004`: chase stops at melee and faces the victim. `TP-SL06-006`: autostore loot slot → `SMSG_ITEM_PUSH_RESULT` received=0 + `SMSG_LOOT_REMOVED`. `TP-SL06-007`: solo `CMSG_LOOT_MONEY` → copper + empty `SMSG_LOOT_CLEAR_MONEY` (no notify). `TP-SL06-008`: after `respawnDelayMs`, corpse respawns alive at full HP + nearby `UPDATE_OBJECT`/`VALUES` for `UNIT_FIELD_HEALTH`. `TP-SL06-009`: leash evade → `SMSG_ATTACKSTOP` (packed creature→player, `nowDead=0`) + full-HP VALUES. Attack/loot on piep still needs a client click. `TP-SL06-010`: a killing creature swing runs `DeathHandler.killPlayer` (`SMSG_FORCE_MOVE_ROOT`, `PLAYER_FIELD_BYTES` release-timer bit, 6-min timer auto-repops and unroots). `TP-SL06-011/012`: melee kill → `Player.giveXp` (`XpFormulas` = Formulas.h) → `SMSG_LOG_XPGAIN` + `PLAYER_XP`; ding → `SMSG_LEVELUP_INFO`, level/stats/next-XP re-derived from `LevelStats`, talent point from 10. `TP-SL06-013`: respawn delay = `urand(spawntimesecsmin, spawntimesecsmax)` from the `creature` row. `TP-SL06-014`: creature hit → `SMSG_ATTACKERSTATEUPDATE` + victim `UNIT_FIELD_HEALTH` VALUES to nearby players. `TP-SL06-015`: corpse HEALTH 0 + per-viewer `UNIT_DYNAMIC_FLAGS` (`Combat.dynamicFlagsFor`, LOOTABLE for the tapper only) to everyone in view. **Not `p0_client`.** |
| 7 Spells | p0_junit | Gherkin Fireball 133 + JUnit Heroic Strike **78**. SCHOOL_DAMAGE magic miss base **4%** (`SMSG_SPELLLOGMISS` + `SMSG_SPELL_GO` miss row; in-memory always-hit so `@tp-sl07-001` stays deterministic). Lab 8606: rage bar, melee rage, HS 78 on piep (still not `p0_client` in the two-client sense; single-client HS click done). `TP-SL07-011`: a spell kill runs the same `Unit::Kill` path as melee (XP log, loot, respawn, death scripts). `TP-SL07-003`: Fireball/Lesser Heal rank 1 run a 1500 ms cast bar (`SMSG_SPELL_START` timer, GO + TakePower from `SpellEngine.update` in `World.tick`). `TP-SL07-004/012`: moving during the cast bar cancels it (`SMSG_CAST_RESULT` INTERRUPTED, `SMSG_SPELL_FAILURE` + `SMSG_SPELL_FAILED_OTHER` to the set). `TP-SL07-005`: GCD 1500 ms category 133; Frost Nova **122** RecoveryTime **25 s** at `Spell::cast` → `NOT_READY`; remaining CD survives logout (`character_spell_cooldown` / `PlayerPersist`) and is written in login `SMSG_INITIAL_SPELLS` (no `SMSG_SPELL_COOLDOWN` on a normal cast). `TP-SL07-006`: Frost Armor **168** writes `UNIT_FIELD_AURA[0]` + FLAGS/LEVELS/APPLICATIONS VALUES and `SMSG_UPDATE_AURA_DURATION` 1_800_000 ms. `TP-SL07-007`: after DurationIndex the world tick clears `UNIT_FIELD_AURA[slot]` to 0 (`AURA_REMOVE_BY_EXPIRE`; holder gone). `TP-SL07-008`: UA **30108** Amplitude **3000** ms from `World.tick` sends `SMSG_PERIODICAURALOG` + target `UNIT_FIELD_HEALTH` VALUES. `TP-SL07-009`: Drain Life **689** `MSG_CHANNEL_START` packed caster + spell + **5000** ms; `CMSG_CANCEL_CHANNELLING` → `MSG_CHANNEL_UPDATE` time **0**. `TP-SL07-010`: `CMSG_CANCEL_CAST` during a Fireball bar → `SPELL_FAILED_INTERRUPTED` + `SMSG_SPELL_FAILURE`, no GO. `CMSG_USE_ITEM` Hearthstone **6948** ON_USE **8690** → `SMSG_SPELL_START` timer **10000** ms (CastingTimeIndex 7). `CMSG_CANCEL_AUTO_REPEAT_SPELL` clears CURRENT_AUTOREPEAT (no `SMSG_CANCEL_AUTO_REPEAT`). **Not `done`.** |
| 8 Content | p0_junit | Gherkin Goldshire NPC 240, vendor 54, quest 783 (`@tp-sl08-003` accept Willem 823 → turn-in McBride 197, `SMSG_QUESTGIVER_QUEST_COMPLETE` questId). `TP-SL08-004`: `MovementType` 1 + `spawndist` OOC `SMSG_MONSTER_MOVE` walk 2.5. `TP-SL08-005`: one walk spline `last_idx` 1; dest Z from `maps/*.map` when DataDir present. `TP-SL08-006`: Corina **54** `SMSG_GOSSIP_MESSAGE` vendor icon **1**, in-memory menu **0** / `DEFAULT_GOSSIP_MESSAGE` **0x00FFFFFF** (CMaNGOS menu 0). `TP-SL08-007`: `CMSG_NPC_TEXT_QUERY` → `SMSG_NPC_TEXT_UPDATE` 8 slots; missing `npc_text` is `"Greetings $N"`; SQL `npc_text` loads when present. `TP-SL08-008`: gossip select vendor option → `SMSG_LIST_INVENTORY`. `TP-SL08-009`: Llane **911** gossip select trainer icon **3** → `SMSG_TRAINER_LIST` spell **6673**, greeting mangos_string **51**. `CMSG_TRAINER_LIST` uses the same encoder. `TP-SL08-010`: Olivia **2455** gossip select money-bag icon **6** → `SMSG_SHOW_BANK`; `CMSG_BANKER_ACTIVATE` uses the same encoder. `TP-SL08-011`: Dungar **352** gossip select taxi icon **2** → `SMSG_SHOWTAXINODES` unk **1**, curloc Stormwind **2**, taximask 16×uint32. `TP-SL08-012`: Farley **295** gossip select interact icon **5** → empty `SMSG_GOSSIP_COMPLETE` + `SMSG_BINDER_CONFIRM` raw guid (confirm only; bind is `CMSG_BINDER_ACTIVATE` / spell **3286**). `TP-SL08-013`: Chilton **8670** gossip select money-bag icon **6** → `MSG_AUCTION_HELLO` raw guid, house **1** (faction **12**). `TP-SL08-014`: Farley **295** `CMSG_BINDER_ACTIVATE` → `SMSG_BINDPOINTUPDATE` at player xyz/map + `SMSG_PLAYERBOUND` npc guid + `SMSG_TRAINER_BUY_SUCCEEDED` guid/spell **3286** + empty `SMSG_GOSSIP_COMPLETE`. `TP-SL08-015`: Farley **295** gossip select `"What can I do at an inn?"` (`GOSSIP_OPTION_GOSSIP`, `action_menu_id` **1221**) → `SMSG_GOSSIP_MESSAGE` menu **1221** / text **1853** / gossipItemCount **0**. `TP-SL08-016`: `GOSSIP_OPTION_GOSSIP` with `action_menu_id` < 0 → empty `SMSG_GOSSIP_COMPLETE`. `TP-SL08-017`: `GOSSIP_OPTION_GOSSIP` with `action_poi_id` **1** (Lion's Pride Inn) → `SMSG_GOSSIP_POI` first (flags **99**, x **-9459**, y **42.0805**, icon **7**, data **0**, name C-string); missing POI id sends nothing. `TP-SL08-018`: `gossip_menu_option.condition_id` != 0 omitted from `SMSG_GOSSIP_MESSAGE` (v1 does not interpret `conditions`). Live NPCs come from map-0 spawns; `.tele` to Goldshire (`-9465 16 57`). Continent `gameobject` rows instantiate on map 0/1 (event GOs stay off that list). Gossip leftover list is empty. `TP-SL08-023`: `CMSG_QUESTGIVER_CHOOSE_REWARD` A Threat Within **783** → `SMSG_QUESTGIVER_QUEST_COMPLETE` xp **40** + `PLAYER_XP`; Rest and Relaxation **2158** RewItemId1 Refreshing Spring Water **159** × **5** + `SMSG_ITEM_PUSH_RESULT` (max level: xp **0**, money += RewMoneyMaxLevel). `TP-SL08-024`: quest **18** choose-reward index **1** stores Militia Hammer **5580**, not Dagger **2224** (`RewChoiceItemId[1]`; index ≥ **6** ignored). `TP-SL08-025`: `CMSG_QUESTGIVER_REQUEST_REWARD` **783** → `SMSG_QUESTGIVER_OFFER_REWARD` raw guid, title, enable-next **1**, empty emotes/items (`PlayerMenu::SendQuestGiverOfferReward`). `TP-SL08-026`: `CMSG_QUESTLOG_REMOVE_QUEST` slot **0** → `PLAYER_QUEST_LOG_1_1..3` VALUES **0** (`HandleQuestLogRemoveQuest` → `SetQuestSlot(slot, 0)`). `TP-SL08-027`: `CMSG_SET_WATCHED_FACTION` **72** → `PLAYER_FIELD_WATCHED_FACTION_INDEX` VALUES. `CMSG_SET_FACTION_INACTIVE` Stormwind list **19** → next login `SMSG_INITIALIZE_FACTIONS` slot **19** `FACTION_FLAG_INACTIVE`. `CMSG_SET_FACTION_ATWAR` Booty Bay list **1** → next login `SMSG_INITIALIZE_FACTIONS` slot **1** `FACTION_FLAG_AT_WAR`. `TP-SL08-019`: Willem **823** `CMSG_QUESTGIVER_STATUS_QUERY` → `SMSG_QUESTGIVER_STATUS` status **6** (`DIALOG_STATUS_AVAILABLE`). `TP-SL08-020`: `CMSG_QUESTGIVER_STATUS_MULTIPLE_QUERY` → `SMSG_QUESTGIVER_STATUS_MULTIPLE` (Willem **823** AVAILABLE **6**; creatures without `UNIT_NPC_FLAG_QUESTGIVER` omitted). `TP-SL08-021`: kill Kobold Vermin **6** with quest **7** accepted → `SMSG_QUESTUPDATE_ADD_KILL` (count 1..10 / required **10**) and `SMSG_QUESTUPDATE_COMPLETE` at 10. `TP-SL08-022`: loot Red Burlap Bandana **752** with quest **18** accepted → `SMSG_QUESTUPDATE_ADD_ITEM` (item **752**, add-count **12**; C++ has no quest id) and `SMSG_QUESTUPDATE_COMPLETE` at 12. **Not `p0_client`.** |
| 9 Social / economy | p0_junit | Gherkin two doubles: invite/`SMSG_GROUP_LIST` (HIGHGUID_GROUP 0x1F50), party chat 0x02, trade both-accept, who display cap 49, `character_social` + `SMSG_CONTACT_LIST` after relog, mail take-item **uint32** guid-low. `TP-SL09-005`: guild invite `SMSG_GUILD_INVITE` then accept `GE_JOINED`. `TP-SL09-006`: Chilton **8670** `CMSG_AUCTION_SELL_ITEM` → `STARTED`/`OK`. `TP-SL09-007`: `CMSG_AUCTION_PLACE_BID` → `BID_PLACED`/`OK` + outbid. `TP-SL09-008`: `CMSG_DEL_FRIEND` → `FRIEND_REMOVED` **0x05**. `TP-SL09-009`: take-money, delete, mark-as-read, return-to-sender, Plain Letter **8383**, `MSG_QUERY_NEXT_MAIL_TIME` unread senders or float **−86400**. Live 8606: trade window opens (REMI/REMI2). Party/mail still need a client click. `TP-SL09-010`: `CMSG_ADD_IGNORE` → `FRIEND_IGNORE_ADDED` **0x0F**; `CMSG_DEL_IGNORE` → `FRIEND_IGNORE_REMOVED` **0x10**; `CMSG_SET_CONTACT_NOTES` on next `SMSG_CONTACT_LIST`; `CMSG_SET_TRADE_GOLD` → partner `SMSG_TRADE_STATUS_EXTENDED` money u32 + accept `PLAYER_FIELD_COINAGE`. `TP-SL09-011`: `CMSG_GROUP_DECLINE` → inviter `SMSG_GROUP_DECLINE` name; `CMSG_GROUP_UNINVITE` / `CMSG_GROUP_UNINVITE_GUID` → kicked empty `SMSG_GROUP_UNINVITE` + empty `SMSG_GROUP_LIST`; `CMSG_GROUP_SET_LEADER` → `SMSG_GROUP_SET_LEADER` name + `SMSG_GROUP_LIST`; `CMSG_REQUEST_PARTY_MEMBER_STATS` → `SMSG_PARTY_MEMBER_STATS_FULL` packed guid + mask **0x00040BFF**. **Not `p0_client`.** |
| 10 Instances / PvP | p0_junit | Gherkin RFC AT **2230** map 389 shared bind + reset; WSG WAIT_JOIN **2** then port map **489**. `TP-SL10-004`: solo L70 `MSG_SET_DUNGEON_DIFFICULTY` mode **1** → 12-byte S2C. `TP-SL10-005`: 6th new instance/hour → `TRANSFER_ABORTED` **389**/`0x03`. `TP-SL10-006`: `CMSG_LEAVE_BATTLEFIELD` → entry-point `SMSG_NEW_WORLD` (combat ignored). Slice 10 Build filled on the wire. **Not `p0_client`.** |
| 11 EventAI | p0_junit | Gherkin Garrick **103** spell **7164** (`TP-SL11-001`); `FactorySelector.selectAI` (ScriptName / pet / AIName / Guard / permit EventAI / Null); creature melee after `BASEATTACKTIME` in range; straight-line MotionMaster CHASE + `SMSG_MONSTER_MOVE` (no mmaps); RANGE / OOC_LOS with vmap-off LOS clear; `EventAiStore` JDBC `creature_ai_scripts` (103 seed if empty); dbscripts CAST **15** on creature death, delay queue + TERMINATE **31** (no buddy search). **Not `p0_client`.** |
| 12 Combat math | p0_junit | `MeleeTableTest` 5% miss/dodge/parry/block vs same-level NPC; yellow-NPC miss; miss vs player `× 0.04`; dual-wield white +19 miss unless next-melee queued; NPC dodge 0.1 / parry 0.6 skill; NPC block no +skill when defense ahead; player avoid from `PLAYER_*_PERCENTAGE` (no +5); expertise `× 0.25` off dodge/parry (`PLAYER_OFFHAND_EXPERTISE` on offhand); offhand crit from `PLAYER_OFFHAND_CRIT_PERCENTAGE`; totem victim base miss 0; partial block subtracts `PLAYER_SHIELD_BLOCK` and `SMSG_ATTACKERSTATEUPDATE` blocked uint32 is the remainder; player vs NPC>10 glance 10+(defense−skill), wand-user <30 `level+(defense−skill)`; glance damage 1.2/1.3 or caster 0.9/0.6; sitting player remaining table is crit; player/creature crit 2×; NPC crush 150% when skill deficit ≥ 15; evade mode is `MELEE_HIT_EVADE` (`hitInfo` MISS\|SWINGNOHITSOUND, `VictimState` EVADES); swing damage from `UNIT_FIELD_MINDAMAGE`/`MAXDAMAGE`; offhand from `MIN/MAXOFFHANDDAMAGE` without consuming next-melee; `hitInfo` CRITICALHIT / CRUSHING / NOACTION on next-melee spell swing / LEFTSWING on offhand (kept on miss); dual-wield offhand auto-attack after `UNIT_FIELD_BASEATTACKTIME+1` (`HITINFO_LEFTSWING`); melee range `ATTACK_DISTANCE` 5 + `UNIT_FIELD_COMBATREACH`; `ThreatManager` per-attacker float, white melee ×1, highest guid retarget, reset on evade; Heroic Strike **78** queues next-melee with SpellInfo weapon bonus; the swing consumes it and adds that bonus to the weapon range |
| 13 Spell catalogs | p0_junit | `SpellEngine.knownEffect` + dummy/script no crash. SCHOOL_DAMAGE Fireball **133** uses magic miss **4%**, not the melee die (`TP-SL07-001` / `TP-SL13-001`). LEARN_SPELL adds `SpellInfo.misc` (Fireball **133**) to the player's spell book. CREATE_ITEM adds Worn Shortsword **25** and `SMSG_ITEM_PUSH_RESULT` with `created=1`. TRIGGER_SPELL nests cataloged Fireball **133** (no GCD). `CMSG_CANCEL_AURA` unapplies UA **30108**. UA **30108** Amplitude tick sends `SMSG_PERIODICAURALOG` (`PERIODIC_DAMAGE` **3**, damage = health delta). Lesser Heal **2050** crit uses `PLAYER_SPELL_CRIT_PERCENTAGE1` at **1.5×**. `EFFECT_HEAL_MAX_HEALTH` **67** (`apply`) restores `UNIT_FIELD_MAXHEALTH`. |
| 14 Living world | p0_junit | Wire: `Slice14P0Test` swap VALUES (`TP-SL14-001`) including `CMSG_SWAP_ITEM` backpack, destroy Worn Shortsword **25**, split **25**, Olivia **2455** `SMSG_SHOW_BANK`, autobank/autostore **25**, autoequip **25** to mainhand **15**, autoequip Small Brown Pouch **4496** `SMSG_OPEN_CONTAINER`, autostore bag **25** to next backpack slot, set ammo Rough Arrow **2512** `PLAYER_AMMO_ID`, read Dusty Unsent Letter **889** (`PageText` **16**) `SMSG_READ_ITEM_OK` raw guid, open Dented Crate **6351** `SMSG_LOOT_RESPONSE` pickpocketing **2**, wrap Worn Shortsword **25** with Red Ribboned Wrapping Paper **5042** (`ITEM_DYNFLAG_WRAPPED`, consume 1 from a stack; equipped mainhand **15** is ignored; stack count ≠ 1 is ignored; already wrapped is ignored; Small Brown Pouch **4496** bag is ignored; soulbound is ignored; unique MaxCount is ignored; channeling is ignored), open wrapped **25** unwraps (no loot), `CMSG_CANCEL_TEMP_ENCHANTMENT` mainhand **15** clears `TEMP_ENCHANTMENT_SLOT` (no reply), Llane **6673** (`002`), Dungar taxi (`003`), Elwynn weather (`004`). `GameEventMgr.start`/`stop` Midsummer **1** spawns/despawns Luma Skymother **25697** (SQL `game_event_creature` guid **5470211**, else seed guid 11) and Ice Stone **187882**; SQL `game_event_gameobject` Ice Block **188067** guid **5470021**. Positive event guid stay off the continent spawn list until `start`. GO-only events still spawn (Midsummer Ice Stone **187882** without a creature list). Continent `gameobject` rows instantiate on map 0/1 (Ice Stone **187882** guid **5470020**). `TP-SL14-005`: Olivia **2455** `CMSG_BUY_BANK_SLOT` → `SMSG_BUY_BANK_SLOT_RESULT` **3** (`ERR_BANKSLOT_OK`), `PLAYER_BYTES_2` byte 2 = 1, first slot **1000** copper (`BankBagSlotPrices.dbc`). `TP-SL14-006`: `WUPDATE_AUCTIONS` 60s removes listings with `timeLeftMs` ≤ 0 without a player. `TP-SL14-007`: `ChangeWeatherInterval` 10 min no-chances path sends `SMSG_WEATHER` fine. `TP-SL14-008`: due `GameEventMgr` schedule starts Midsummer on the events timer. `TP-SL14-009`: warrior `CMSG_LEARN_TALENT` **124** rank 0 → `SMSG_LEARNED_SPELL` **12282**. `TP-SL14-010`: `WUPDATE_CORPSES` 20 min then `MSG_CORPSE_QUERY` found **0**. `TP-SL14-011`: `WUPDATE_GROUPS` 1 s promotes offline leader (`SMSG_GROUP_SET_LEADER`). `TP-SL14-012`: `WUPDATE_DELETECHARS` 1 day purges soft-deleted chars older than 30 days. Slice 14 Build leftover is filled. **Hollow (gap-inventory):** `TP-SL14-013` leftover spell-hit/crit/haste swap/destroy/unequip, `SPELL_AURA_MOD_ATTACK_POWER` field, `stat_type8..10` / `dmg_min4..5` / holy resist (no TBC item with HolyRes). SQL `fire_res` through `arcane_res`, `dmg_min2`, `stat_type6`, `stat_type7`, `block`, and `dmg_min3` load. Autoequip HIT/DEFENSE/DODGE/PARRY/BLOCK/CRIT/RESILIENCE/HASTE/EXPERTISE ratings write combat-rating VALUES; swap-inv, swap-item, destroy, and autostore unequip send HIT, DEFENSE, DODGE, PARRY, BLOCK, CRIT, RESILIENCE, HASTE, and EXPERTISE. Seventh proto slot: autoequip Vengeful Gladiator's Dragonhide Tunic **33675** CRIT **19**. Spell hit: autoequip Auchenai Anchorite's Robe **29341** CR_HIT_SPELL **23**. Spell crit: autoequip Garments of Serene Shores **34229** CR_CRIT_SPELL **25**. Spell haste: autoequip Sunglow Vest **34212** CR_HASTE_SPELL **33**. Shield block: autoequip Worn Wooden Shield **2362** `PLAYER_SHIELD_BLOCK` **1**; SQL `block` loads. Melee crit: autoequip Cloak of Darkness **33122** CR_CRIT_MELEE **24** (not ranged). Ranged crit: autoequip Netherstrand Longbow **30318** CR_CRIT_RANGED **50** (not melee). Third damage line: query Twin Blades of Azzinoth **18582** arcane **40–60**; SQL `dmg_min3` loads. Melee hit: autoequip Tom's Boots 1 **32954** CR_HIT_MELEE **15**. Ranged hit: autoequip Tom's Boots 1 **32954** CR_HIT_RANGED **15** (melee stays **15**). Item health: autoequip Test HP Ring **6673** `UNIT_FIELD_MAXHEALTH` **1**. Item mana: autoequip Test MP Ring **6674** `UNIT_FIELD_MAXPOWER1` **105**. ON_EQUIP: autoequip Band of the Eternal Champion **29301** `UNIT_FIELD_AURA[0]` **14052**; autostore unequip clears it. `TP-SL14-014` closed on 8606 (`tpSl14CancelMountAuraDismount` packed guid to self+nearby; `CMSG_GAMEOBJ_REPORT_USE` is not an 8606 opcode). Not `p0_client`. |
| 15 Social leftover | p0_junit | Wire: `Slice15P0Test` NBG roll timeout 60000 (`TP-SL15-001`), raid `GROUP_LIST` + ready-check requester guid (`002`), roster gender (`003`), guild bank `SMSG_GUILD_BANK_LIST` (`004`), Chilton 8670 `BuildAuctionInfo` + delay 300 (`005`). `TP-SL15-006/007/008`: Rebecca Laughlin **5193** guild charter **5863** buy/sign/turn-in. `TP-SL15-009`: `MSG_RAID_TARGET_UPDATE` set+list (icons). `TP-SL15-010`: `CMSG_AUCTION_LIST_OWNER_ITEMS` → `SMSG_AUCTION_OWNER_LIST_RESULT` own rows only (delay **300**); `CMSG_AUCTION_LIST_BIDDER_ITEMS` → `SMSG_AUCTION_BIDDER_LIST_RESULT` current bids (delay **300**); `CMSG_AUCTION_REMOVE_ITEM` → `AUCTION_REMOVED`/`OK`, item mailed (`25:0:5`), bidder `SMSG_AUCTION_REMOVED_NOTIFICATION` + refund mail (`25:0:4`). Slice 15 Build leftover filled. Not `p0_client`. |
| 16 PvP leftover | p0_junit | Wire: `Slice16P0Test` arena `SMSG_NEW_WORLD` ∈ {559,562,572} (`001`), Hellfire WS **2480**/2476/2478 (`002`), LFG packed-guid list (`003`), WSG pickup WS **1545** + aura **23333** (`004`), drop cancel-aura → WS **-1** (`005`), capture AT **3646**/**3647** → WS **0** + captures **1581**/**1582** (`006`). Slice 16 Build WSG flags filled. Not `p0_client`. |
| 17 Death | p0_junit | Wire: `Slice17P0Test` GY + delay (`001`), reclaim 50% (`002`), spirit healer **15007** (`003`), closest GY + ghost flags (`004`), `CMSG_SELF_RES` (`005`), resurrect-response accept HP (`006`), KillPlayer **6 min** auto-repop (`007`), KillPlayer equipped durability **10%** VALUES + empty `SMSG_DURABILITY_DAMAGE_DEATH` (`008`; backpack/bank untouched), repair at a smith `DurabilityCosts`×quality (Worn Shortsword 10 lost → **8**c) and broke does nothing (`009`). Slice 17 Build filled on the wire. Not `p0_client`. |
| 18 Pets | p0_junit | Wire: `Slice18P0Test` `SMSG_PET_SPELLS` pet guid (`001`), stable **0x0A/0x08/0x09** (`002`), warlock dismiss vs hunter (`003`), totem `SMSG_DESTROY_OBJECT` (`004`), pet `COMMAND_ATTACK` → `SMSG_ATTACKERSTATEUPDATE` damage (`005`). `TP-SL18-006` rename: hunter `CMSG_PET_RENAME` → `UNIT_FIELD_PET_NAME_TIMESTAMP` + `UNIT_FIELD_BYTES_2` rename flag cleared + name-query refresh; empty/too-long → `SMSG_PET_NAME_INVALID` **3**/**4**. `CMSG_PET_SET_ACTION` rewrites a known-spell slot and `SMSG_PET_SPELLS` (Fire Shield **2947**); command/reaction count-1 ignored. `CMSG_PET_SPELL_AUTOCAST` toggles ACT_ENABLED/**0xC1** vs ACT_DISABLED/**0x81** on that slot. `CMSG_PET_CAST_SPELL` Fireball **133** → `SMSG_SPELL_START` caster = pet; instant Frost Nova **122** also `SMSG_SPELL_GO`. `CMSG_PET_STOP_ATTACK` after `COMMAND_ATTACK` → `SMSG_ATTACKSTOP` packed pet→prey, `nowDead` **0**. `CMSG_PET_CANCEL_AURA` Frost Armor **168** → pet `UNIT_FIELD_AURA` **0**; dead pet → `SMSG_PET_ACTION_FEEDBACK` **1**. `CMSG_REQUEST_PET_INFO` resends `SMSG_PET_SPELLS`. `TP-SL18-006` leftover opcodes filled. Not `p0_client`. |
| 19 Channels | p0_junit | Wire: `Slice19P0Test` YOU_JOINED **0x02** (`001`), list (`002`), text emote (`003`), voice ignored (`004`), YOU_LEFT (`005`), password **0x07/0x04** (`006`), moderator not-member **0x05** (`007`). `TP-SL19-008` `CMSG_CHANNEL_OWNER` → `CHANNEL_OWNER` **0x0B** + name (not-member **0x05**); custom-channel `CMSG_CHANNEL_SET_OWNER` → `OWNER_CHANGED` **0x08** + guid (not-owner **0x0A**, missing **0x09**). `CMSG_CHANNEL_UNMODERATOR` → `MODE_CHANGE` **0x0C** (not-moderator **0x06**). `CMSG_CHANNEL_MUTE` → `MODE_CHANGE` muted **0x08**. `CMSG_CHANNEL_UNMUTE` → `MODE_CHANGE` clears mute. `CMSG_CHANNEL_INVITE` → `INVITE` **0x18** to the target and `PLAYER_INVITED` **0x1D** to the inviter. `CMSG_CHANNEL_KICK` → `PLAYER_KICKED` **0x12**. `CMSG_CHANNEL_BAN` → `PLAYER_BANNED` **0x14** and rejoin `BANNED` **0x13**. `CMSG_CHANNEL_UNBAN` → `PLAYER_UNBANNED` **0x15**. `CMSG_CHANNEL_ANNOUNCEMENTS` → `ANNOUNCEMENTS_OFF/ON` **0x0E/0x0D**. `CMSG_CHANNEL_MODERATE` → `MODERATION_ON/OFF` **0x0F/0x10**. `TP-SL19-008` leftover opcodes filled. Join/leave/password/moderation on the wire. Join/leave/password/moderation on the wire. |
| 20 Items leftover | p0_junit | Wire: `Slice20P0Test` buyback slot **74** VALUES (`001`), socket bonus vs meta **25890** (`002`), repair coinage (`003`), buy-in-slot bagslot placement (`004`). |
| 21 Guild leftover | p0_junit | Wire: `Slice21P0Test` … ranks: `CMSG_GUILD_RANK` (`015`), `ADD_RANK` (`016`), `DEL_RANK` (`017`). Slice 21 Build ranks filled. Not `p0_client`. |
| 22 Honor / inspect / duel | p0_junit | Wire: `Slice22P0Test` `SMSG_PVP_CREDIT` + cap 75000 (`001`), inspect packed + 0x3D (`002`), duel **3000** + OOB (`003`), `CMSG_SET_TITLE` → `PLAYER_CHOSEN_TITLE` (`004`), `MSG_INSPECT_ARENA_TEAMS` (`005`), `CMSG_TOGGLE_PVP` desired+UNIT_FLAG_PVP (`006`), duel melee packed `ATTACKERSTATEUPDATE` + 1 HP `DUEL_COMPLETE`/`DUEL_WINNER` (`007`). Slice 22 Build filled. **Hollow (gap-inventory):** `CMSG_DUEL_CANCELLED` dropped — `TP-SL22-008`. Not `p0_client`. |
| 23 Arena teams | p0_junit | Wire: `Slice23P0Test` arena CRUD (`005`–`010`), BG positions (`011`), battlefield list (`012`), area spirit TIME (`013`). Slice 23 Build filled. Not `p0_client`. |
| 24 AV / AB / EY | p0_junit | AB five nodes; EY flag + 2s tower tick (`010`); AV Snowfall/mine/death/Vanndar end (`009`). Slice 24 Build filled. Not `p0_client`. |
| 25 Outdoor PvP | p0_junit | Silithyst; EP four towers; ZM East+West+GY (banner+32430→33779/969); Halaa; TF lock. Slice 25 Build filled on wire. Not `p0_client`. |
| 26 Spell algorithms | p0_junit | Named packets; INSTAKILL/LEECH/DRAIN/env-dmg/weapon-noschool/weapon-percent/dispel/dispel-mechanic/power-burn/threat/heal-pct/energize-pct/inebriate/quest-fail/self-res/heal-mechanical/destroy-totems/durability-damage/knock-back/modify-threat-pct/reputation/durability-pct/dual-wield/parry/block/spawn/proficiency/distract/combo/interrupt/sanctuary/extra attacks/bind/taunt/quest-complete/resurrect/resurrect-new/spirit-heal/farsight/send-taxi/kill-credit-group/charge/charge-dest/dismiss-pet/play-music/play-sound/pull-towards/pull-towards-dest/leap-back/normalized-weapon/spellsteal/unlearn-spec/leap/teleport-units/stuck/learn-pet/summon-dead-pet/summon-player/skinning/pickpocket/feed-pet/disenchant/prospecting/open-lock/open-lock-item/enchant-held-item/enchant-item/enchant-item-temporary/create-pet/force-cast/skin-player-corpse/teleport-graveyard/trigger-spell-2/activate-object/summon-object-slot1/summon-object-slot2/summon-object-wild/tame-creature/summon-pet/force-cast-with-value/trigger-spell-with-value/trigger-missile/summon-change-item/teleport-units-face-caster/duel/trans-door/summon/send-event/area-aura-party/skill-step/summon-raf-friend/knockback-from-position/persistent-area-aura/area-aura-friend/area-aura-enemy/area-aura-pet/area-aura-owner/redirect-threat/language/dodge/trade-skill/skill/weapon/defense/evade/call-pet/pull/spell-defense/bind-sight/stealth-effect/detect/create-house. Effect catalog complete for every id an 8606 spell reaches; spell-less ids excluded per `TP-NEG-010`. Chest loot / talent wipe on wire (`Slice26P0Test`). Aura catalog via `AuraEngine`: mod-stun, mod-root (`SMSG_FORCE_MOVE_ROOT` + order counter). Stun also roots on wire. Still hollow: remaining aura ids 0–261; creature `SMSG_SPLINE_MOVE_ROOT`; aura removal / unroot. |
| 27 Transports | p0_junit | Board MO; leaveMoTransport; ONTRANSPORT; FORCE speed ACKs; mount; summon response; spline done counter. Slice 27 Build filled on wire. Not `p0_client`. |
| 28 Misc packets | p0_junit | Wire: `Slice28P0Test` quest push guid (`001`), master loot `SMSG_ITEM_PUSH_RESULT` (`002`), GMTICKET **0x06** (`003`), LFG accept `SMSG_LFG_UPDATE` (`004`). |
| 29 GM commands | p0_junit | `Slice29P0Test`: `.help`/`.dismount` SEC_PLAYER; `.appear` LowerSecurity; SQL overlay. |
| 30 ScriptDevAI registry | p0_junit | `Slice30P0Test`: `boss_gruul` Growth **36300**; missing ScriptName fallback. |
| 31 TBC raids | p0_junit | Curator **30383**; Magtheridon; Karathress; Al'ar; Anetheron **31306** / 20s; Najentus **39837** (`TP-SL31-007`). |
| 32 TBC 5-mans | p0_junit | Spec file existence for HFC/CF/Auch/TK/CoT |
| 33 Classic raids | p0_junit | MC/BWL/AQ/Naxx/ZG/Onyxia + `world-remaining.md` |
| 34 Class scripts | p0_junit | Execute **5308→20647**; UA **30108**; spec grep |

## Loop B — official 8606 two-client lab

Started after Loop A wire oracles for slices 14–16. GitHub Actions runs `mvn test` only; it does not launch `Wow.exe`. Next lab click: two 8606 clients for `TP-SL04-003` (observer `MSG_MOVE_HEARTBEAT`) and `TP-SL04-005` (other-player create + name query). Until that lab, slice 4 stays `p0_client` not `done`. Do not tick `p0_client` on later slices from JUnit alone.
